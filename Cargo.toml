[workspace]
members = [
    "main",
]
resolver = "2"

[profile.dev]
codegen-units = 16
debug = 1
incremental = true
lto = "off"
opt-level = 3
panic = "abort"
strip = "none"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = 3
panic = "abort"
strip = true
